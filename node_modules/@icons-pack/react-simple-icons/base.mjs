import { jsxs, jsx } from 'react/jsx-runtime';
import * as React from 'react';

const baseIcon = (iconTitle, hex, iconPath) => React.forwardRef(function({ title = iconTitle, color = "currentColor", size = 24, ...others }, ref) {
  if (color === "default") {
    color = hex;
  }
  return /* @__PURE__ */ jsxs(
    "svg",
    {
      xmlns: "http://www.w3.org/2000/svg",
      width: size,
      height: size,
      fill: color,
      viewBox: "0 0 24 24",
      ref,
      ...others,
      children: [
        /* @__PURE__ */ jsx("title", { children: title }),
        /* @__PURE__ */ jsx("path", { d: iconPath })
      ]
    }
  );
});
const baseIcon$1 = baseIcon;

export { baseIcon$1 as default };
